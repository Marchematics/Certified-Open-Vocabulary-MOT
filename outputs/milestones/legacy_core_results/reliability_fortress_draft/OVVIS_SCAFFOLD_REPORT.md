# OVVIS Mask-Path Certification Scaffold

This is a box-to-mask scaffold over BURST paths. Each box is treated as a rectangular mask, and path conflicts are defined by mask IoU. It validates the data interface for mask-path certification but is not a full LV-VIS/OVVIS mask benchmark result.
