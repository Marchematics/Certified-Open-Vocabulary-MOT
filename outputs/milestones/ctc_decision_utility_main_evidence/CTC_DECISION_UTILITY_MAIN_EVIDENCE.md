# CTC Decision-Utility Main Evidence

This milestone reframes the completed strict CTC evidence as release-time decision utility. The learned-hybrid source releases under strict `alpha=0.10` with zero held-out FTR, while destroyed-ranking and noisy/high-volume controls are refused before false lineage edges enter the downstream graph. The learned raw prefix is clean at the strict budgets, so the positive claim is release certification rather than fixed-size reranking improvement.
