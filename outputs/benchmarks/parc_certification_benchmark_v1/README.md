# PARC Certification Benchmark v1

Public-safe benchmark artifacts for PARC-Track certification. This directory contains audit labels, result tables, schema documentation, and a tiny fixture. It intentionally excludes raw videos, raw annotations, model weights, and detector/tracker caches.

Use `tiny_fixture/` to validate installation before materializing any external dataset.
