# Latest Experiment Delta

The latest public state is no longer a phase-delta package. It is the final
release artifact set:

- `outputs/milestones/reliability_fortress/`
- `outputs/milestones/generality_reliability/`
- `outputs/benchmarks/parc_certification_benchmark/`
- `outputs/milestones/legacy_core_results/`

Use `MANIFEST_SHA256.txt` for repository-wide integrity checks and the
per-directory manifests for individual frozen milestones.
